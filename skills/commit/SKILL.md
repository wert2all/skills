---
name: commit
description: generate commit message and commit all changes.
disable-model-invocation: true
---

Use `git` subagent, which should use `/caveman-commit` skill to generate commit message and commit all changes, using generated commit message.

Do NOT run any tests or checks in this skill.
